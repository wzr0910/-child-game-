# 技术架构

> 持续完善中

---

## 1. 整体架构图

```
┌─────────────────┐
│   用户浏览器     │
└────────┬────────┘
         │ HTTPS
         ▼
┌─────────────────────────────────────┐
│        Next.js (前端 + 后端一体)     │
│  ┌─────────────┐  ┌──────────────┐ │
│  │  React 页面  │  │ API Routes   │ │
│  │  (App Router)│  │ Server Actions│ │
│  └──────┬──────┘  └──────┬───────┘ │
└─────────┼────────────────┼─────────┘
          │                │
          ▼                ▼
   ┌──────────────┐  ┌──────────────┐
   │  Supabase    │  │  AI 大模型    │
   │  (PostgreSQL │  │  API         │
   │  + Auth)     │  │  (DeepSeek等) │
   └──────────────┘  └──────────────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  AI 生图 API  │
                     │  (豆包/即梦)  │
                     └──────────────┘
```

## 2. 技术选型

| 层 | 候选方案 | 推荐选择 | 理由 |
|---|---|---|---|
| **前端框架** | Next.js / Vue / 纯HTML | **Next.js 14** | 自带前后端，新手友好，部署简单 |
| **后端** | Next.js 内置 / Flask / Express | **Next.js API Routes** | 不用单独搭后端，一套搞定 |
| **数据库** | Supabase / SQLite / MongoDB | **Supabase** | 免费额度够用，自带用户系统 |
| **AI 大模型** | DeepSeek / 通义千问 / GPT-4 | **待定** | 看价格、稳定性和中文能力 |
| **AI 生图** | 豆包 / 即梦 / Stable Diffusion | **待定** | 看价格和风格匹配度 |
| **部署** | Vercel / 自建 | **Vercel** | 免费、Next.js 官方支持 |
| **认证** | Supabase Auth / NextAuth | **Supabase Auth** | 和数据库一套 |

## 3. 项目目录结构

```
portfolio-project/
├── app/                      # Next.js 页面（App Router）
│   ├── page.tsx             # 首页
│   ├── read/                # 阅读页面
│   │   └── [chapter]/       # 动态章节
│   ├── chat/                # 对话页面
│   └── layout.tsx           # 全局布局
│
├── components/               # 复用组件
│   ├── ChatWindow.tsx       # 对话窗口
│   ├── ChapterReader.tsx    # 章节阅读器
│   └── PersonaAvatar.tsx    # 角色头像
│
├── lib/                      # 工具函数
│   ├── ai/                  # AI 调用封装
│   │   ├── chat.ts          # 对话 API
│   │   └── image.ts         # 生图 API
│   ├── db/                  # 数据库操作
│   │   └── supabase.ts      # Supabase 客户端
│   └── prompts/             # Prompt 模板
│       ├── zarathustra.ts   # 查拉图斯特拉角色 prompt
│       └── life-advice.ts   # 生活应用 prompt
│
├── public/                   # 静态资源
│   └── images/              # 图片
│
├── docs/                     # 补充资料
│
├── PRD.md
├── 架构.md
├── progress.md
└── README.md
```

## 4. 关键技术决策记录 (ADR)

### 决策 1：为什么选 Next.js（不只是前端）
> 待补充。核心点：前后端一体、文件路由清晰、API Routes 简单、部署免费

### 决策 2：为什么选 Supabase 而不是自建数据库
> 待补充。核心点：免运维、自带 Auth、有免费额度

### 决策 3：AI Prompt 设计思路
> 待补充。核心点：角色设定 + Few-shot + 输出格式约束

## 5. 第三方依赖清单

| 依赖 | 用途 | 成本 |
|---|---|---|
| Next.js | 全栈框架 | 免费 |
| Supabase | 数据库 + 认证 | 免费额度 |
| Tailwind CSS | 样式 | 免费 |
| AI 大模型 API | 对话能力 | 按调用付费 |
| AI 生图 API | 配图 | 按调用付费 |

## 6. 环境变量

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
AI_API_KEY=
AI_BASE_URL=
IMAGE_API_KEY=
IMAGE_BASE_URL=
```
